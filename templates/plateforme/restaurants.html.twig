{% extends 'base.html.twig' %}

{% block title %}Restaurateurs - BudgEat{% endblock %}

{% block body %}

<style>

    /* Navbar */

    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #fff;
        border-bottom: 2px solid #ffc01e;
    }
    .navbar .logo img {
        height: 150px;
    }
    .navbar ul {
        list-style: none;
        display: flex;
        gap: 5rem;
        margin: 0;
        padding: 0;
    }
    .navbar ul li a {
        text-decoration: none;
        color: #333;
        font-weight: bold;
    }
    .navbar .buttons a {
        padding: 0.5rem 1rem;
        border-radius: 5px;
        font-weight: bold;
        text-decoration: none;
    }
    .navbar .btn-login {
        background-color: white;
        color: #ffc01e;
        border: 2px solid #ffc01e;
        margin: 2rem;
    }
    .navbar .btn-signup {
        background-color: #ffc01e;
        color: white;
        border: 2px solid #ffc01e;
    }

    /* Hero Section */
    .hero {
        display: flex;
        background-color: #ffc01e;
        padding: 2rem;
        color: #333;
        flex-direction : row;
        justify-content: space-around;
    }
    .hero-content {
        max-width: 50%;
        padding: 0 4.5rem;
    }
    .hero-content h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }
    .hero-content p {
        font-size: 1.2rem;
    }
    /* Conteneur principal du carrousel */
.carousel {
    position: relative;
    width: 100%;
    max-width: 600px; /* Limite la largeur */
    margin: 0 auto; /* Centré horizontalement */
    overflow: hidden; /* Cache les images non visibles */
    border-radius: 10px; /* Arrondi les bords du carrousel */
}

/* Images dans le carrousel */
.carousel-images {
    display: flex;
    transition: transform 0.5s ease-in-out; /* Animation de défilement */
}

.carousel-images img {
    width: 100%;
    flex-shrink: 0; /* Empêche le rétrécissement des images */
    object-fit: cover; /* Maintient les proportions de l'image */
    height: 300px;
}

/* Boutons de navigation */
.carousel-buttons {
    position: absolute;
    top: 50%;
    width: 100%;
    display: flex;
    justify-content: space-between;
    transform: translateY(-50%);
    pointer-events: none; /* Empêche les boutons de bloquer le clic sur les images */
}

.carousel-buttons button {
    background-color: rgba(0, 0, 0, 0.5);
    border: none;
    color: white;
    font-size: 2rem;
    padding: 0.5rem 1rem;
    cursor: pointer;
    pointer-events: auto; /* Active le clic pour les boutons */
    transition: background-color 0.3s ease;
    z-index: 10;
}

.carousel-buttons button:hover {
    background-color: rgba(0, 0, 0, 0.8); /* Changement de couleur au survol */
}

/* Indicateurs (petits points) */
.carousel-indicators {
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 10px;
}

.carousel-indicators span {
    width: 12px;
    height: 12px;
    background-color: #fff;
    border-radius: 50%;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.carousel-indicators span.active {
    background-color: #333;
}

    /* BudgEat and Restaurants */
    .budget, .restaurants {
        padding: 2rem;
        text-align: center;
    }

    .budget {
        text-align: center;
        margin: 30px auto;
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 500px;
    }
    
    .budget h2 {
        font-size: 24px;
        margin-bottom: 20px;
        font-weight: bold;
        color: #333;
    }
    
    .budget-input {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }
    
    .budget-btn {
        background-color:#ffc01e;
        color: white;
        border: none;
        font-size: 18px;
        font-weight: bold;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    
    .budget-btn:hover {
        background-color: #ffc01e;
    }
    
    #budget-display {
        font-size: 18px;
        font-weight: bold;
        text-align: center;
        width: 80px;
        border: 2px solid #ffc01e;
        border-radius: 5px;
        padding: 5px;
        color: #333;
        background-color: #fff;
        pointer-events: none; /* Rend le champ non éditable */
    }
    
    .btn-login {
        background-color: #28a745;
        color: white;
        border: none;
        font-size: 16px;
        font-weight: bold;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    
    .btn-login:hover {
        background-color: #ffc01e;
        color: white;
    }
    
    .btn-signup:hover {
        background-color:white;
        color:#ffcc00;
    }

    .hess {
        display: flex;
        justify-content: center; 
        align-items: center; 
        position: relative; 
    }
    
    .hess h2 {
        margin: 0; 
        font-size: 1.5rem;
        color: #333;
        text-align: center;
        flex-grow: 1; 
    }
    
    .hess button {
        position: absolute;
        right: 0; 
        background-color:#ffc01e;
        color: #fff;
        border-radius: 5px;
        padding: 0.5rem 1rem;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
        font-weight: bold;
    }
    
    .hess button:hover {
        background-color:#ffc01e; 
    }
    
    .restaurants { padding: 2rem; text-align: center; }
    .restaurants h2 { margin-bottom: 2rem; }
    .restaurant-list { display: flex; gap: 1rem; justify-content: center; }
    .restaurant-card { background-color: #fff; border: 1px solid #ddd; border-radius: 10px; width: 200px; text-align: center; padding: 1rem; }
    .restaurant-card img{ width: 80%; height: 180px; border-radius: 10px; margin-bottom: 1rem; }
    .restaurant-card button, .budget-input button { margin-top: 0.5rem; padding: 0.5rem 1rem; border-radius: 5px; background-color: #ffcc00; color: white; font-weight: bold; }

/* Footer */
.footer {
    background-color: #f9f9f9; 
    color: #333333; 
    padding-top: 40px;
    width: 100%;
    text-align: left; 
    border-top: 1px solid #eee; 
    box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.05);
     
}

.footer .footer-content {
    display: flex;
    flex-wrap: wrap;
    align-items: flex-start;
    justify-content: space-between;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

.footer-logo {
    margin-right: 40px;
    flex: 1 1 150px; 
}

.footer-logo .logo {
    width: 180px; 
    height: auto;
}


.footer-links-container {
    display: flex;
    gap: 40px;
    justify-content: space-between;
    flex: 3 1 500px; 
    flex-wrap: wrap; 
}

.footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
}

.footer-links li {
    margin-bottom: 12px; 
}

.footer-links a {
    color: #333333; 
    text-decoration: none;
    font-size: 1.1rem; 
    font-weight: 500; 
    transition: color 0.3s ease, padding-left 0.3s ease;
}

.footer-links a:hover {
    color: #ffcc00; 
    padding-left: 8px; 
}



.social-icons {
    display: flex;
    gap: 25px;
    align-items: center;
    justify-content: flex-start;
    margin-top: 30px;
    flex: 1 1 180px; 
}

.social-icon {
    font-size: 36px; 
    color: #333333;
    text-decoration: none;
    transition: color 0.3s ease, transform 0.3s ease;
}

.social-icon:hover {
    color: #ffc01e; 
    transform: scale(1.1); 
}


.footer-copyright {
    margin-top: 30px;
    font-size: 14px;
    text-align: center;
    color: #777;
    padding-top: 20px; 
    border-top: 1px solid #eee; 
}

@media (max-width: 768px) {
    .footer .footer-content {
        flex-direction: column;
        text-align: center; 

    .footer-links-container {
        justify-content: center;
        gap: 20px;
    }

    .footer-logo {
        margin: 0 auto 20px; 
    }

    .social-icons {
        justify-content: center;
        margin-top: 20px;
    }

    .footer-links {
        margin-bottom: 20px;
    }

    .footer-copyright {
        margin-top: 40px;
        font-size: 12px;
    }
}
}

#myChart{
    border: transparent solid black;
    padding: 2%;
    width: 50%;  /* Taille responsive */
    height: 50%;
    background: white;
    border-radius: 11px;
}

.buttonAbonner {
    border: 3px solid #fff;
    border-radius: 50px;
    background: linear-gradient(45deg, #ffcc00, #ff9100); /* Dégradé moderne */
    padding: 1rem 2rem;
    font-size: 1.5rem;
    color: #fff;
    font-weight: bold;
    cursor: pointer;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
    text-transform: uppercase;
}

.buttonAbonner:hover {
    background: linear-gradient(45deg, #ff9100, #ffcc00); 
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); 
    transform: translateY(-5px); 
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    color: #333; 
    border-color: #ff9100; 
}
.buttonAbonner:active {
    transform: translateY(0);
    box-shadow: 0 0 20px rgba(255, 145, 0, 0.6);
}


.pBody{
    background: #ede8e8;
    padding-bottom: 4%;
    padding-top: 4%;
}

.sectionBody{
    border: 1px solid transparent;
    margin: 0 3%;
}

    
</style>
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
<!-- Navbar -->
<div class="navbar">
    <div class="logo">
        <a href="/"><img src="/assets/logo.png" alt="BudgEat Logo"></a >
    </div>
    <ul>
        <li><a href="/restaurants">Restaurants</a ></li>
        <li><a href="/contact">Contact</a ></li>
    </ul>
    {% if app.user %}
        <div class="buttons">
            <a href="{{ path('app_logout') }}" class="btn-login">Déconnexion</a> <!-- Lien vers la route de déconnexion -->
            <a>{{ app.user.getNom() }}</a>
        </div>
    {% else %}
        <div class="buttons">
            <a href="/clients/connexion" class="btn-login">Connexion</a >
            <a href="/clients/inscription" class="btn-signup">Inscription</a >
        </div>
    {% endif %}
</div>

<div class="hero">
    <h1 style="color: white; font-weight: bold;">Abonnez-vous pour faire partie de la meilleure communauté</h1>
    <div id="subscribeButton" class="buttonAbonner" onclick="toggleSubscription()">
        S'abonner
    </div>
</div>


<!--Gerer : IF abonner afficher dashboard-->

<div class="pBody">

    
    <div class = "sectionBody"><canvas id="myChart"></canvas></div>

    

</div>
    <!-- Footer -->
                        <footer class="footer">
                            <div
                                class="footer-content">
                                <!-- Logo -->
                                <div class="footer-logo">
                                    <img src="/assets/logo.png" alt="Logo" class="logo">
                                </div>

                                <!-- Colonnes de liens -->
                                <div class="footer-links-container">
                                    <ul class="footer-links">
                                        <li>
                                            <strong>
                                                <a href="#">Explorer</a>
                                            </strong>
                                        </li>
                                        <li>
                                            <a href="#">Crée un compte</a>
                                        </li>
                                        <li>
                                            <a href="#">Accueil</a>
                                        </li>
                                        <li>
                                            <a href="#">Connexion</a>
                                        </li>
                                    </ul>
                                    <ul class="footer-links">
                                        <li>
                                            <strong>
                                                <a href="#">Ressource</a>
                                            </strong>
                                        </li>
                                        <li>
                                            <a href="#">Contact</a>
                                        </li>
                                        <li>
                                            <a href="#">Aide</a>
                                        </li>
                                        <li>
                                            <a href="#">Adhérer à nos restaurants</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                            <!-- Icônes sociales sous le logo -->
                            <div class="social-icons">
                                <a href="#" class="social-icon">
                                    <i class="fab fa-instagram"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-twitter"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-youtube"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-linkedin"></i>
                                </a>
                            </div>

                            <!-- Copyright -->
                            <p class="footer-copyright">&copy; 2025 Budgeat. Tous droits réservés.</p>
                        </footer>
</div>

<script>

 function toggleSubscription() {
        var button = document.getElementById("subscribeButton");

        // Si le texte est "S'abonner", on le change en "Désabonner", et inversement
        if (button.innerHTML === "S'abonner") {
            button.innerHTML = "Désabonner";
        } else {
            button.innerHTML = "S'abonner";
        }
    }
    //chart.js
    document.addEventListener('DOMContentLoaded', function () {
            var ctx = document.getElementById('myChart').getContext('2d');
            var myChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: ['Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet','Août', 'Septembre','Octobre','Novembre','Décembre'],
                    datasets: [{
                        label: 'Nombre de commandes',
                        data: [12, 19, 3, 5, 2, 3, 7, 8, 12, 4, 2, 1],
                        borderColor: 'rgb(255, 255, 255)',
                        backgroundColor: [
                        'rgba(255, 173, 49, 0.64)',
                    ],
                        fill: false,
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    }
                    
                }
            });
        });

   
</script>
{% endblock %}
