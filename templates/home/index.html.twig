{% extends 'base.html.twig' %}

{% block title %}Home - BudgEat{% endblock %}

{% block body %}
<style>

    /* Navbar */

    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 1rem;
        background-color: #fff;
        border-bottom: 2px solid #ffcc00;
    }
    .navbar .logo img {
        height: 150px;
    }
    .navbar ul {
        list-style: none;
        display: flex;
        gap: 5rem;
        margin: 0;
        padding: 0;
    }
    .navbar ul li a {
        text-decoration: none;
        color: #333;
        font-weight: bold;
    }
    .navbar .buttons a {
        padding: 0.5rem 1rem;
        border-radius: 5px;
        font-weight: bold;
        text-decoration: none;
    }
    .navbar .btn-login {
        background-color: white;
        color: #ffcc00;
        border: 2px solid #ffcc00;
        margin: 2rem;
    }
    .navbar .btn-signup {
        background-color: #ffcc00;
        color: white;
        border: 2px solid #ffcc00;
    }

    /* Hero Section */
    .hero {
        display: flex;
        align-items: center;
        background-color: #ffcc00;
        padding: 2rem;
        color: #333;
    }
    .hero-content {
        max-width: 50%;
        padding: 0 4.5rem;
    }
    .hero-content h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }
    .hero-content p {
        font-size: 1.2rem;
    }
    /* Conteneur principal du carrousel */
.carousel {
    position: relative;
    width: 100%;
    max-width: 600px; /* Limite la largeur */
    margin: 0 auto; /* Centré horizontalement */
    overflow: hidden; /* Cache les images non visibles */
    border-radius: 10px; /* Arrondi les bords du carrousel */
}

/* Images dans le carrousel */
.carousel-images {
    display: flex;
    transition: transform 0.5s ease-in-out; /* Animation de défilement */
}

.carousel-images img {
    width: 100%;
    flex-shrink: 0; /* Empêche le rétrécissement des images */
    object-fit: cover; /* Maintient les proportions de l'image */
    height: 300px;
}

/* Boutons de navigation */
.carousel-buttons {
    position: absolute;
    top: 50%;
    width: 100%;
    display: flex;
    justify-content: space-between;
    transform: translateY(-50%);
    pointer-events: none; /* Empêche les boutons de bloquer le clic sur les images */
}

.carousel-buttons button {
    background-color: rgba(0, 0, 0, 0.5);
    border: none;
    color: white;
    font-size: 2rem;
    padding: 0.5rem 1rem;
    cursor: pointer;
    pointer-events: auto; /* Active le clic pour les boutons */
    transition: background-color 0.3s ease;
    z-index: 10;
}

.carousel-buttons button:hover {
    background-color: rgba(0, 0, 0, 0.8); /* Changement de couleur au survol */
}

/* Indicateurs (petits points) */
.carousel-indicators {
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 10px;
}

.carousel-indicators span {
    width: 12px;
    height: 12px;
    background-color: #fff;
    border-radius: 50%;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.carousel-indicators span.active {
    background-color: #333;
}

    /* BudgEat and Restaurants */
    .budget, .restaurants {
        padding: 2rem;
        text-align: center;
    }

    .budget {
        text-align: center;
        margin: 30px auto;
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 500px;
    }
    
    .budget h2 {
        font-size: 24px;
        margin-bottom: 20px;
        font-weight: bold;
        color: #333;
    }
    
    .budget-input {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }
    
    .budget-btn {
        background-color: #ffcc00;
        color: white;
        border: none;
        font-size: 18px;
        font-weight: bold;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    
    .budget-btn:hover {
        background-color: #e6b800;
    }
    
    #budget-display {
        font-size: 18px;
        font-weight: bold;
        text-align: center;
        width: 80px;
        border: 2px solid #ffcc00;
        border-radius: 5px;
        padding: 5px;
        color: #333;
        background-color: #fff;
        pointer-events: none; /* Rend le champ non éditable */
    }
    
    .btn-login {
        background-color: #28a745;
        color: white;
        border: none;
        font-size: 16px;
        font-weight: bold;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    
    .btn-login:hover {
        background-color: #ffcc00;
        color: white;
    }
    
    .btn-signup:hover {
        background-color:white;
        color:#ffcc00;
    }

    .hess {
        display: flex;
        justify-content: center; /* Centre horizontalement */
        align-items: center; /* Aligne verticalement */
        position: relative; /* Nécessaire pour positionner le bouton à droite */
    }
    
    .hess h2 {
        margin: 0; /* Supprime les marges par défaut */
        font-size: 1.5rem;
        color: #333;
        text-align: center;
        flex-grow: 1; /* Permet à l'élément de prendre tout l'espace restant */
    }
    
    .hess button {
        position: absolute;
        right: 0; /* Place le bouton à droite */
        background-color: #ffcc00;
        color: #fff;
        border-radius: 5px;
        padding: 0.5rem 1rem;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
        font-weight: bold;
    }
    
    .hess button:hover {
        background-color: #e6b800; /* Couleur au survol */
    }
    
    .restaurants { padding: 2rem; text-align: center; }
    .restaurants h2 { margin-bottom: 2rem; }
    .restaurant-list { display: flex; gap: 1rem; justify-content: center; }
    .restaurant-card { background-color: #fff; border: 1px solid #ddd; border-radius: 10px; width: 200px; text-align: center; padding: 1rem; }
    .restaurant-card img{ width: 80%; height: 180px; border-radius: 10px; margin-bottom: 1rem; }
    .restaurant-card button, .budget-input button { margin-top: 0.5rem; padding: 0.5rem 1rem; border-radius: 5px; background-color: #ffcc00; color: white; font-weight: bold; }

.footer {
    background-color: #ffffff; / Fond blanc /
    color: rgb(0, 0, 0); / Texte noir /
    padding: 20px 0; / Espacement autour du footer /
    width: 100%;
    text-align: left; / Aligner le texte à gauche dans tout le footer /
}

.footer .footer-content {
    display: flex; / Utilisation de Flexbox pour aligner le contenu horizontalement /
    align-items: flex-start; / Aligner les éléments en haut du footer /
    justify-content: space-between; / Espacer le logo à gauche et les liens à droite /
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

.footer-logo {
    margin-right: 40px; / Espacement entre le logo et les colonnes de liens /
}

.footer-logo .logo {
    width: 150px; / Largeur du logo /
    height: auto; / Conserver les proportions /
}

/ Container pour les colonnes de liens /
.footer-links-container {
    display: flex;
    gap: 40px; / Espacement entre les colonnes de liens /
    justify-content: flex-start; / Aligner les colonnes à gauche /
    width: 100%; / Prendre toute la largeur du footer /
}

/ Liens dans chaque colonne /
.footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
}

.footer-links li {
    margin-bottom: 10px; / Espacement entre les liens /
}

.footer-links a {
    color: #000000; / Couleur des liens /
    text-decoration: none; / Supprimer le soulignement /
}

.footer-links a:hover {
    text-decoration: underline; / Ajouter un soulignement au survol /
}

/ Icônes sociales sous le logo, alignées à droite et plus grandes /
.social-icons {
    display: flex; / Utilisation de Flexbox pour aligner horizontalement les icônes /
    gap: 20px; / Espacement entre les icônes /
    align-items: center; / Centrer verticalement les icônes /
    margin-top: 30px; / Augmenter l'espacement au-dessus des icônes /
    justify-content: flex-end; / Aligner les icônes à droite /
    height: 60px; / Augmenter la hauteur du conteneur des icônes /
}

/ Style pour les icônes sociales /
.social-icon {
    font-size: 32px; / Augmenter la taille des icônes /
    color: #000000; / Couleur des icônes /
    text-decoration: none; / Supprimer le soulignement des liens /
}

.social-icon:hover {
    color: #fdce54; / Couleur au survol des icônes /
}

/ Ajuster l'espacement entre les icônes pour qu'elles soient bien positionnées /
.social-icons a {
    display: flex;
    align-items: center; / Aligner les icônes verticalement dans leur espace /
    justify-content: center; / Centrer les icônes horizontalement /
    height: 100%; / Prendre toute la hauteur disponible dans le conteneur /
}

/ Footer copyright /
.footer-copyright {
    margin-top: 20px;
    font-size: 14px;
    text-align: center; / Aligner le texte du copyright à gauche */
}

    
</style>
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
<!-- Navbar -->
<div class="navbar">
    <div class="logo">
        <a href="/"><img src="/assets/logo.png" alt="BudgEat Logo"></a >
    </div>
    <ul>
        <li><a href="/restaurants">Restaurants</a ></li>
        <li><a href="/contact">Contact</a ></li>
    </ul>
    <div class="buttons">
        <a href="/clients/connexion" class="btn-login">Connexion</a >
        <a href="/clients/inscription" class="btn-signup">Inscription</a >
    </div>
</div>

<!-- Hero Section -->
<div class="hero">
    <div class="hero-content">
        <h1>Mangez Malin</h1>
        <p>Trouvez les meilleurs restos près de chez vous,</br> adaptés à votre budget !</p >
    </div>
    <div class="carousel">
        <div class="carousel-images">
            <img src="/assets/subway.jpg" alt="Image 1">
            <img src="/assets/burger.jpg" alt="Image 2">
            <img src="/assets/bk.jpg" alt="Image 3">
        </div>
        <div class="carousel-buttons">
            <button class="prev">&lt;</button>
            <button class="next">&gt;</button>
        </div>
        <div class="carousel-indicators">
            <span class="active" data-slide="0"></span>
            <span data-slide="1"></span>
            <span data-slide="2"></span>
        </div>
    </div>
</div>

<!-- Budget Section -->
<div class="budget">
    <h2>C’est quoi ton budget, chef ?</h2>
    <form method="GET" action="{{ path('filter_restaurants') }}">
        <div class="budget-input">
            <button type="button" class="budget-btn" id="decrease">-</button>
            <input type="number" name="budget" id="budget-display" value="10" readonly>
            <button type="button" class="budget-btn" id="increase">+</button>
            <button type="submit" class="btn-login">Valider</button>
        </div>
    </form>
</div>

<!-- Restaurants Section -->
<div class="restaurants">
    <div class="hess"><h2>Quand c’est la hess...</h2> <a href="/restaurants"><button>Voir plus</button></a></div>
    <div class="restaurant-list">
        <div class="restaurant-card">
            <img src="/assets/matcha.jpg" alt="Ochaya">
            <h3>Restaurants Ochaya</h3>
            <p>4,7 ★ - 50 m</p >
            <button>Menu</button>
            <button>Trajet</button>
        </div>
        <div class="restaurant-card">
            <img src="/assets/kodawari.jpg" alt="Kodawari Tsukiji">
            <h3>Kodawari Tsukiji</h3>
            <p>4,0 ★ - 3 km</p >
            <button>Menu</button>
            <button>Trajet</button>
        </div>
        <div class="restaurant-card">
            <img src="/assets/ponpon.jpg" alt="Ponpon Mochi Donuts">
            <h3>Ponpon Mochi</h3>
            <p>4,8 ★ - 5 km</p >
            <button>Menu</button>
            <button>Trajet</button>
        </div>
    </div>
    <!-- Footer -->
                        <footer class="footer">
                            <div
                                class="footer-content">
                                <!-- Logo -->
                                <div class="footer-logo">
                                    <img src="{{ asset('images/logo.png') }}" alt="Logo" class="logo">
                                </div>

                                <!-- Colonnes de liens -->
                                <div class="footer-links-container">
                                    <ul class="footer-links">
                                        <li>
                                            <strong>
                                                <a href="#">Explorer</a>
                                            </strong>
                                        </li>
                                        <li>
                                            <a href="#">Crée un compte</a>
                                        </li>
                                        <li>
                                            <a href="#">Accueil</a>
                                        </li>
                                        <li>
                                            <a href="#">Connexion</a>
                                        </li>
                                    </ul>
                                    <ul class="footer-links">
                                        <li>
                                            <strong>
                                                <a href="#">Ressource</a>
                                            </strong>
                                        </li>
                                        <li>
                                            <a href="#">Contact</a>
                                        </li>
                                        <li>
                                            <a href="#">Aide</a>
                                        </li>
                                        <li>
                                            <a href="#">Adhérer à nos restaurants</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                            <!-- Icônes sociales sous le logo -->
                            <div class="social-icons">
                                <a href="#" class="social-icon">
                                    <i class="fab fa-instagram"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-twitter"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-youtube"></i>
                                </a>
                                <a href="#" class="social-icon">
                                    <i class="fab fa-linkedin"></i>
                                </a>
                            </div>

                            <!-- Copyright -->
                            <p class="footer-copyright">&copy; 2025 Budgeat. Tous droits réservés.</p>
                        </footer>
</div>

<script>
    const images = document.querySelectorAll('.carousel-images img');
    const carouselImages = document.querySelector('.carousel-images');
    const indicators = document.querySelectorAll('.carousel-indicators span');
    const prevButton = document.querySelector('.prev');
    const nextButton = document.querySelector('.next');

    let currentIndex = 0;

    function updateCarousel() {
        carouselImages.style.transform = `translateX(-${currentIndex * 100}%)`;
        indicators.forEach((indicator, index) => {
            indicator.classList.toggle('active', index === currentIndex);
        });
    }

    nextButton.addEventListener('click', () => {
        currentIndex = (currentIndex + 1) % images.length;
        updateCarousel();
    });

    prevButton.addEventListener('click', () => {
        currentIndex = (currentIndex - 1 + images.length) % images.length;
        updateCarousel();
    });

    indicators.forEach((indicator, index) => {
        indicator.addEventListener('click', () => {
            currentIndex = index;
            updateCarousel();
        });
    });

    updateCarousel();

    document.addEventListener("DOMContentLoaded", () => {
        const decreaseButton = document.getElementById("decrease");
        const increaseButton = document.getElementById("increase");
        const budgetDisplay = document.getElementById("budget-display");
    
        decreaseButton.addEventListener("click", () => {
            let currentValue = parseInt(budgetDisplay.value);
            if (currentValue > 0) {
                budgetDisplay.value = currentValue - 5;
            }
        });
    
        increaseButton.addEventListener("click", () => {
            let currentValue = parseInt(budgetDisplay.value);
            budgetDisplay.value = currentValue + 5;
        });
    });
   
</script>
{% endblock %}